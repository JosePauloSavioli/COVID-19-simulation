pip install lmfit
pip install numpy
pip install SkillMetrics
pip install scipy
pip install matplotlib
pip install pandas
pip install psutil
pip install plotly [OLCA tutorial para instalação -> https://github.com/plotly/orca no readme]
pip install statsmodels
pip install p-tqdm (pode ser underline)
